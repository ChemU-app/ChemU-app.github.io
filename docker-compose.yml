services:
  mongo:
    image: mongo:7
    container_name: chemu_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    command: ["--replSet", "rs0", "--bind_ip_all"]
    healthcheck:
      test: ["CMD-SHELL", "mongosh --eval \"db.adminCommand('ping')\" --quiet"]
      interval: 5s
      timeout: 5s
      retries: 12
    volumes:
      - mongo_data:/data/db

  mongo-setup:
    image: mongo:7
    depends_on:
      mongo:
        condition: service_healthy
    restart: "no"
    entrypoint: >
      mongosh --host mongo:27017 --eval
      "try {
        rs.status()
      } catch(e) {
        rs.initiate({
          _id: 'rs0',
          members: [{ _id: 0, host: 'mongo:27017' }]
        })
      }"

  server:
    build: ./server
    container_name: chemu_server
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env.production
    depends_on:
      mongo:
        condition: service_healthy

volumes:
  mongo_data:
